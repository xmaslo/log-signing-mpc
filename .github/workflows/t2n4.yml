name: threshold-2-number-of-parties-4

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-evaluation@v4
        with:
          python-version: '3.11'

      - name: Build the Docker image
        run: docker compose build build-service-24

      - name: Run servers
        run: docker-compose -f docker-compose-24.yml up -d

      - name: Wait for servers to start
        run: |
          retries=0
          max_retries=10
          while [[ $retries -lt $max_retries ]]; do
            sleep 5
            if curl --silent --head --fail http://la1:8001 && \
               curl --silent --head --fail http://la2:8002 && \
               curl --silent --head --fail http://la3:8003 && \
               curl --silent --head --fail http://la4:8004; then
              break
            fi
            retries=$((retries+1))
          done

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r python/tests/requirements.txt

      - name: Run API tests
        run: |
          pytest -k "TestKeyGen24 or TestSigning24 or TestVerify24"

      - name: Clean up
        run: docker compose down
